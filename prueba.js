// esta es una prueba.

var base=4;
var altura=3;
const NUM=2;

var area;

area = base * altura / NUM;
alert (area);
console.log(area);
document.write (area);

switch (key) {
    case value:
        
        break;

    default:
        break;
}